<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <?php

   /* $myfile = fopen("sample.text","r");
    echo fread($myfile,filesize("sample.text"));
    fclose($myfile);*/

    /*$myfile = fopen("sample.text","r");
    echo fgets($myfile);
    fclose($myfile);*/

   /* $myfile = fopen("newfile.text","w");

    $text = "ali, ahmadi \n";
    fwrite($myfile,$text);
    $text = "azia, ahmazizaadi \n";
    fwrite($myfile,$text);
    fclose($myfile)*/

   // rename("newfile.text","file.txt")

   //copy("newfile.text","file.txt");
   unlink("sample.txt");


    ?>
</body>
</html>